name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  github-pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@latest
      - uses:  helaili/jekyll-action@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
